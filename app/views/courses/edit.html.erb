<h2>Edit course:</h2>

<%= render 'form' %>

<%= link_to '<i class="glyphicon glyphicon-arrow-left"></i> Back'.html_safe, courses_path, {:style => 'color:#0079bf; font-size: 18px; font-weight: bold; font-family: "Ubuntu"'} %>
